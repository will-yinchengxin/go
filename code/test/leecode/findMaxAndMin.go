package leecode

/*
查找数组中 最大值 和 最小值

// 这里的初始化的值根据情况而定
minNum := 0
maxNum := 14

if nums[i] < maxNum {
		maxNum = nums[i]
}
if nums[i] > minNum {
	minNum = nums[i]
}
// 如果 minNum - maxNum > 0 交换一下, 否则正常执行即可
if minNum - maxNum > 0 {
	return minNum - maxNum < 5
}
return maxNum - minNum < 5
*/
